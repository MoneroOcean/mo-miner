name: Stability

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: stability-${{ github.ref }}
  cancel-in-progress: true

env:
  MOM_PORTABLE_BUILD: "1"
  WINDOWS_ONEAPI_URL: https://raw.githubusercontent.com/oneapi-src/oneapi-ci/master/.github/workflows/build_all.yml
  WINDOWS_ONEAPI_PACKAGE: intel-oneapi-toolkit
  WINDOWS_DPCPP_COMPONENTS: intel.oneapi.win.cpp-dpcpp-common
  ONEAPI_ROOT: C:/Program Files (x86)/Intel/oneAPI

jobs:
  package-linux-x64:
    name: Ubuntu / package
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Free disk space
        run: |
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /usr/local/share/boost \
            /usr/local/.ghcup \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /opt/hostedtoolcache/go \
            /opt/hostedtoolcache/Python \
            /opt/hostedtoolcache/Ruby \
            /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk
          docker system prune --all --force || true
          df -h

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build native addon in release image
        run: ./r.sh node -e "console.log('mom build ready')"

      - name: Package release archive
        run: .github/workflows/scripts/package-linux.sh

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: mom-lin-package
          path: mom-*-lin.tgz

  test-linux-x64:
    name: Ubuntu / ${{ matrix.suite }}
    needs: package-linux-x64
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        suite:
          - cpu
          - sycl-cpu
          - gpu

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Download package
        uses: actions/download-artifact@v7
        with:
          name: mom-lin-package
          path: dist

      - name: Test extracted package
        run: |
          set -euo pipefail
          archive="$(find dist -name 'mom-*-lin.tgz' -print -quit)"
          .github/workflows/scripts/test-release-linux.sh "$archive" "${{ matrix.suite }}"

  package-linux-nvidia-x64:
    name: Ubuntu NVIDIA / package
    runs-on: ubuntu-24.04
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Free disk space
        run: |
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /usr/local/share/boost \
            /usr/local/.ghcup \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /opt/hostedtoolcache/go \
            /opt/hostedtoolcache/Python \
            /opt/hostedtoolcache/Ruby \
            /opt/hostedtoolcache/Java_Temurin-Hotspot_jdk
          docker system prune --all --force || true
          df -h

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build native addon in NVIDIA (DPC++ CUDA) image
        run: ./r.sh --nvidia node -e "console.log('mom nvidia build ready')"

      - name: Package release archive
        run: .github/workflows/scripts/package-linux-nvidia.sh

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: mom-lin-nvidia-package
          path: mom-*-lin-nvidia.tgz

  test-linux-nvidia-x64:
    name: Ubuntu NVIDIA / ${{ matrix.suite }}
    needs: package-linux-nvidia-x64
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    # Only the pure-CPU (xmrig) suite runs: the dpcpp-cuda build exposes no CPU SYCL
    # device and CI runners have no NVIDIA GPU, so SYCL/GPU algos are verified on real
    # NVIDIA hardware instead.
    strategy:
      fail-fast: false
      matrix:
        suite:
          - cpu

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Download package
        uses: actions/download-artifact@v7
        with:
          name: mom-lin-nvidia-package
          path: dist

      - name: Test extracted package
        run: |
          set -euo pipefail
          archive="$(find dist -name 'mom-*-lin-nvidia.tgz' -print -quit)"
          .github/workflows/scripts/test-release-linux.sh "$archive" "${{ matrix.suite }}"

  package-windows-x64:
    name: Windows / package
    runs-on: windows-2022
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Use oneAPI installation cache
        uses: actions/cache@v5
        id: cache-oneapi
        with:
          path: |
            ${{ env.ONEAPI_ROOT }}
            C:/Program Files/Microsoft Visual Studio/2022/Enterprise/MSBuild/Microsoft/VC/v170/Platforms/*/PlatformToolsets/Intel*
            C:/Program Files/Microsoft Visual Studio/2022/Enterprise/MSBuild/Microsoft/VC/v170/BuildCustomizations/Intel*
            C:/Program Files/Microsoft Visual Studio/2022/Enterprise/MSBuild/Microsoft/VC/v170/Intel*.dll
          key: oneapi-windows-2026.0.0-toolkit-dpcpp-online-v3

      - name: Install Intel oneAPI
        if: steps.cache-oneapi.outputs.cache-hit != 'true'
        shell: cmd
        run: .github\workflows\scripts\install-oneapi.bat "%WINDOWS_ONEAPI_URL%" "%WINDOWS_DPCPP_COMPONENTS%" "%WINDOWS_ONEAPI_PACKAGE%"

      - name: Build native addon
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File .github\workflows\scripts\build-windows.ps1

      - name: Package release archive
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File .github\workflows\scripts\package-windows.ps1

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: mom-win-package
          path: mom-*-win.zip

  test-windows-x64:
    name: Windows / ${{ matrix.suite }}
    needs: package-windows-x64
    runs-on: windows-2022
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        suite:
          - cpu
          - sycl-cpu
          - gpu

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24.15.0
          package-manager-cache: false

      - name: Download package
        uses: actions/download-artifact@v7
        with:
          name: mom-win-package
          path: dist

      - name: Install Intel OpenCL CPU runtime
        if: matrix.suite == 'sycl-cpu'
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File .github\workflows\scripts\install-opencl-runtime-windows.ps1

      - name: Test extracted package
        shell: powershell
        run: |
          $archive = Get-ChildItem dist -Filter "mom-*-win.zip" | Select-Object -First 1
          powershell -NoProfile -ExecutionPolicy Bypass -File .github\workflows\scripts\test-release-windows.ps1 -Archive $archive.FullName -Suite "${{ matrix.suite }}"
